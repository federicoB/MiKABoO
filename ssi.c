#include "ssi.h"

void SSI_entry_point(){
    //TODO: implement
    
    //forever (until killed)
    while(1){
        //receive a message
        //handle service request
        //reply to the message (if necessary)
    }
}